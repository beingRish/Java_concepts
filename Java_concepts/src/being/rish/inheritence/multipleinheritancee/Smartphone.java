package being.rish.inheritence.multipleinheritancee;

public class Smartphone extends Camera {

    // Java does not support multiple inheritance with classes (i.e., a class cannot extend more than one class) mainly to avoid ambiguity and maintain simplicity in the design of the language.
    // While Java doesn't allow multiple class inheritance, it does allow multiple inheritance through interfaces.
}
